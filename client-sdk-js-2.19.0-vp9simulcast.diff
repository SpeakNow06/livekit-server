diff --git a/src/room/PCTransport.ts b/src/room/PCTransport.ts
index 4fbbd75..e1e68c9 100644
--- a/src/room/PCTransport.ts
+++ b/src/room/PCTransport.ts
@@ -23,7 +23,11 @@ process commonly cost more than 10 seconds cause subscriber will get blur video
 the first few seconds. So we use a 70% of target bitrate here as the start bitrate to
 eliminate this issue.
 */
-const startBitrateForSVC = 0.7;
+// SPEAKNOW FORK (2026-06-15): 0.7 → 0.6 — start = istenen (en üst) layer'ın %60'ı.
+// maxbr LocalParticipant'ta artık TOPLAM değil top-layer bitrate'i (FORK #7); 0.6 ile
+// çarpılınca paylaşım ~%60 kaliteyle başlayıp tırmanır (Meet-gibi), toplam yok → donma yok.
+// ESKİ: const startBitrateForSVC = 0.7;
+const startBitrateForSVC = 0.6;
 
 const debounceInterval = 20;
 
diff --git a/src/room/participant/LocalParticipant.ts b/src/room/participant/LocalParticipant.ts
index 1444f18..c1083fe 100644
--- a/src/room/participant/LocalParticipant.ts
+++ b/src/room/participant/LocalParticipant.ts
@@ -14,6 +14,7 @@ import {
   RequestResponse,
   RequestResponse_Reason,
   SimulcastCodec,
+  VideoLayer_Mode,
   SipDTMF,
   SubscribedQualityUpdate,
   TrackInfo,
@@ -1131,18 +1132,22 @@ export default class LocalParticipant extends Participant {
       req.height = dims.height;
       // for svc codecs, disable simulcast and use vp8 for backup codec
       if (isLocalVideoTrack(track)) {
-        if (isSVCCodec(videoCodec)) {
+        // FORK #3 (vp9-simulcast): opts.simulcast=true iken SVC default'ları atlanır —
+        // VP9/AV1, rid-bazlı standart simulcast yoluna girer (Chrome M113+).
+        if (isSVCCodec(videoCodec) && !opts.simulcast) {
           if (track.source === Track.Source.ScreenShare) {
             // vp9 svc with screenshare cannot encode multiple spatial layers
             // doing so reduces publish resolution to minimal resolution
-            opts.scalabilityMode = 'L1T3';
+            // FORK #1: coder'ın modunu ezme — yalnızca boşsa default'la.
+            opts.scalabilityMode = opts.scalabilityMode ?? 'L1T3';
             // Chrome does not allow more than 5 fps with L1T3, and it has encoding bugs with L3T3
             // It has a different path for screenshare handling and it seems to be untested/buggy
             // As a workaround, we are setting contentHint to force it to go through the same
             // path as regular camera video. While this is not optimal, it delivers the performance
             // that we need
             if ('contentHint' in track.mediaStreamTrack) {
-              track.mediaStreamTrack.contentHint = 'motion';
+              // FORK #2: coder'ın hint'ine saygı — yalnızca boşsa 'motion'a düş.
+              track.mediaStreamTrack.contentHint = track.mediaStreamTrack.contentHint || 'motion';
               this.log.debug(
                 'forcing contentHint to motion for screenshare with SVC codecs',
                 getLogContextFromTrack(track),
@@ -1153,12 +1158,17 @@ export default class LocalParticipant extends Participant {
           opts.scalabilityMode = opts.scalabilityMode ?? 'L3T3_KEY';
         }
 
-        req.simulcastCodecs = [
-          new SimulcastCodec({
-            codec: videoCodec,
-            cid: track.mediaStreamTrack.id,
-          }),
-        ];
+        const primaryCodecInfo = new SimulcastCodec({
+          codec: videoCodec,
+          cid: track.mediaStreamTrack.id,
+        });
+        // FORK #4 (vp9-simulcast): mode bildirilmezse server SVC-capable codec'lerde SVC varsayar
+        // (participant.go: MODE_UNUSED + IsMimeTypeStringSVCCapable → MULTIPLE_SPATIAL_LAYERS_PER_STREAM).
+        // Simulcast'i açıkça işaretle ki forwarder rid-bazlı simulcast selector kullansın.
+        if (isSVCCodec(videoCodec) && opts.simulcast) {
+          primaryCodecInfo.videoLayerMode = VideoLayer_Mode.ONE_SPATIAL_LAYER_PER_STREAM;
+        }
+        req.simulcastCodecs = [primaryCodecInfo];
 
         // set up backup
         if (opts.backupCodec === true) {
@@ -1183,6 +1193,12 @@ export default class LocalParticipant extends Participant {
         }
       }
 
+      // FORK #5 (vp9-simulcast): track'in publish modunu işaretle — LocalVideoTrack'teki
+      // katman/dynacast yolları SVC-mi-simulcast-mı kararını codec yerine bundan okur.
+      if (isLocalVideoTrack(track)) {
+        track.svcMode = isSVCCodec(opts.videoCodec) && !opts.simulcast;
+      }
+
       encodings = computeVideoEncodings(
         track.source === Track.Source.ScreenShare,
         req.width,
@@ -1193,7 +1209,8 @@ export default class LocalParticipant extends Participant {
         req.width,
         req.height,
         encodings,
-        isSVCCodec(opts.videoCodec),
+        // FORK #5: simulcast'te rid-bazlı layer sinyali (SVC layer türetmesi değil)
+        isSVCCodec(opts.videoCodec) && !opts.simulcast,
       );
     } else if (track.kind === Track.Kind.Audio) {
       encodings = [
@@ -1248,12 +1265,24 @@ export default class LocalParticipant extends Participant {
               maxbr: encodings[0]?.maxBitrate ? encodings[0].maxBitrate / 1000 : 0,
             });
           }
-        } else if (track.codec && isSVCCodec(track.codec) && encodings[0]?.maxBitrate) {
-          this.engine.pcManager.publisher.setTrackCodecBitrate({
-            cid: req.cid,
-            codec: track.codec,
-            maxbr: encodings[0].maxBitrate / 1000,
-          });
+        }
+        // FORK #7 (GERİ EKLENDİ 2026-06-15, düzeltilmiş): x-google-start-bitrate munging.
+        // ESKİ (v3.1→v3.3'te kaldırılmıştı 2026-06-12): maxbr'a TÜM katmanların TOPLAMINI (~6.3M)
+        //   veriyordu → x-google-start-bitrate ≈ 4.4M (0.7×toplam) → start tahmini yolu aşıyor →
+        //   paylaşım başında 8-10sn donma. ESKİ kod: maxbr = encodings.reduce((s,e)=>s+e.maxBitrate).
+        // YENİ: maxbr = İSTENEN (en üst / tam-kalite) layer'ın bitrate'i (TOPLAM DEĞİL). PCTransport
+        //   startBitrateForSVC=0.6 ile çarpar → x-google-start-bitrate = top layer'ın %60'ı. Böylece
+        //   paylaşım ~%60 kaliteyle başlar (300k BWE default değil) ve oradan tırmanır (Meet-gibi
+        //   anında-net); toplam vermediğimiz için yol aşılmaz → tıkanma/donma yok.
+        if (track.codec && isSVCCodec(track.codec) && encodings.length) {
+          const topBitrate = Math.max(...encodings.map((e) => e.maxBitrate ?? 0));
+          if (topBitrate > 0) {
+            this.engine.pcManager.publisher.setTrackCodecBitrate({
+              cid: req.cid,
+              codec: track.codec,
+              maxbr: topBitrate / 1000,
+            });
+          }
         }
       }
 
diff --git a/src/room/participant/publishUtils.ts b/src/room/participant/publishUtils.ts
index 3d5d648..abf004e 100644
--- a/src/room/participant/publishUtils.ts
+++ b/src/room/participant/publishUtils.ts
@@ -135,7 +135,9 @@ export function computeVideoEncodings(
     videoEncoding.priority,
   );
 
-  if (scalabilityMode && isSVCCodec(videoCodec)) {
+  // FORK #6 (vp9-simulcast): simulcast istendiyse SVC dalına girme — scalabilityMode
+  // aşağıda per-encoding mod olarak kullanılır (M113+ standart yolu).
+  if (scalabilityMode && isSVCCodec(videoCodec) && !useSimulcast) {
     const sm = new ScalabilityMode(scalabilityMode);
 
     const encodings: RTCRtpEncodingParameters[] = [];
@@ -204,6 +206,7 @@ export function computeVideoEncodings(
       sortPresets(options?.videoSimulcastLayers) ?? defaultSimulcastLayers(isScreenShare, original);
   }
   let midPreset: VideoPreset | undefined;
+  let simulcastPresets: VideoPreset[] = [original];
   if (presets.length > 0) {
     const lowPreset = presets[0];
     if (presets.length > 1) {
@@ -221,13 +224,33 @@ export function computeVideoEncodings(
     //      based on other conditions.
     const size = Math.max(width, height);
     if (size >= 960 && midPreset) {
-      return encodingsFromPresets(width, height, [lowPreset, midPreset, original], sourceFramerate);
+      simulcastPresets = [lowPreset, midPreset, original];
+    } else if (size >= 480) {
+      simulcastPresets = [lowPreset, original];
     }
-    if (size >= 480) {
-      return encodingsFromPresets(width, height, [lowPreset, original], sourceFramerate);
+  }
+  const simulcastEncodings = encodingsFromPresets(width, height, simulcastPresets, sourceFramerate);
+  // FORK #6 (vp9-simulcast): Chrome M113+ — VP9/AV1'de standart simulcast'e opt-in için
+  // HER encoding'de scalabilityMode + scaleResolutionDownBy şart; verilmezse Chrome
+  // çoklu encoding'i legacy-SVC olarak yorumlar. opts.scalabilityMode (örn. 'L1T2')
+  // burada per-encoding temporal mod olarak uygulanır.
+  if (isSVCCodec(videoCodec)) {
+    // FORK #8 (v3.4): opts.scalabilityMode TÜM encoding'lere uygulanır (per-encoding,
+    // M113+ standart yol şartı). Classroom 'L1T1' → iki katman da L1T1 (temporal basamak
+    // yok: izleyici ya katmanı tam alır ya alttakine iner); webinar 'L1T2' → eski davranış.
+    // ESKİ (v3.2, 2026-06-12 — aynı gün geri alındı): mod yalnızca üst encoding'e, altlar
+    // sabit L1T2 idi; canlı testte q@T0 basamağı istenmedi (kullanıcı gözlemi: bulanık).
+    // simulcastEncodings.forEach((enc, idx) => {
+    //   /* @ts-ignore */
+    //   enc.scalabilityMode =
+    //     idx === simulcastEncodings.length - 1 ? (scalabilityMode ?? 'L1T2') : 'L1T2';
+    // });
+    for (const enc of simulcastEncodings) {
+      /* @ts-ignore */
+      enc.scalabilityMode = scalabilityMode ?? 'L1T2';
     }
   }
-  return encodingsFromPresets(width, height, [original]);
+  return simulcastEncodings;
 }
 
 export function computeTrackBackupEncodings(
diff --git a/src/room/track/LocalVideoTrack.ts b/src/room/track/LocalVideoTrack.ts
index 34f5dab..3fbeaff 100644
--- a/src/room/track/LocalVideoTrack.ts
+++ b/src/room/track/LocalVideoTrack.ts
@@ -243,6 +243,19 @@ export default class LocalVideoTrack extends LocalTrack<Track.Kind.Video> {
     return items;
   }
 
+  /**
+   * FORK #5 (vp9-simulcast): publish modu — true = SVC (tek stream + scalabilityMode),
+   * false = rid-bazlı simulcast. undefined = eski davranış (codec'ten türet).
+   * LocalParticipant.publishTrack set eder.
+   * @internal
+   */
+  svcMode?: boolean;
+
+  /** @internal */
+  private get isSvcPublish(): boolean {
+    return this.svcMode ?? isSVCCodec(this.codec);
+  }
+
   setPublishingQuality(maxQuality: VideoQuality) {
     const qualities: SubscribedQuality[] = [];
     for (let q = VideoQuality.LOW; q <= VideoQuality.HIGH; q += 1) {
@@ -254,7 +267,7 @@ export default class LocalVideoTrack extends LocalTrack<Track.Kind.Video> {
       );
     }
     this.log.debug(`setting publishing quality. max quality ${maxQuality}`, this.logContext);
-    this.setPublishingLayers(isSVCCodec(this.codec), qualities);
+    this.setPublishingLayers(this.isSvcPublish, qualities);
   }
 
   async restartTrack(options?: VideoCaptureOptions) {
@@ -465,7 +478,10 @@ export default class LocalVideoTrack extends LocalTrack<Track.Kind.Video> {
     });
     // only enable simulcast codec for preference codec setted
     if (!this.codec && codecs.length > 0) {
-      await this.setPublishingLayers(isSVCCodec(codecs[0].codec), codecs[0].qualities);
+      await this.setPublishingLayers(
+        this.svcMode ?? isSVCCodec(codecs[0].codec),
+        codecs[0].qualities,
+      );
 
       return [];
     }
@@ -475,7 +491,7 @@ export default class LocalVideoTrack extends LocalTrack<Track.Kind.Video> {
     const newCodecs: VideoCodec[] = [];
     for await (const codec of codecs) {
       if (!this.codec || this.codec === codec.codec) {
-        await this.setPublishingLayers(isSVCCodec(codec.codec), codec.qualities);
+        await this.setPublishingLayers(this.svcMode ?? isSVCCodec(codec.codec), codec.qualities);
       } else {
         const simulcastCodecInfo = this.simulcastCodecs.get(codec.codec as VideoCodec);
         this.log.debug(`try setPublishingCodec for ${codec.codec}`, {
@@ -556,7 +572,8 @@ export default class LocalVideoTrack extends LocalTrack<Track.Kind.Video> {
           1,
           Math.ceil((this.mediaStreamTrack.getSettings().height ?? 360) / 360),
         ),
-        scalabilityMode: idx === 0 && isSVCCodec(this.codec) ? 'L1T3' : undefined,
+        scalabilityMode:
+          idx === 0 && isSVCCodec(this.codec) ? (this.isSvcPublish ? 'L1T3' : 'L1T2') : undefined,
         maxFramerate: idx === 0 ? 15 : 0,
         maxBitrate: idx === 0 ? e.maxBitrate : 0,
       }));
